export interface vehicle {
  vehicleId: string,
  vehicleStatus: string,
  registrationNumber: string,
  customerId: string,
  customerName: string,
  customerAddress: string,
  isConnected: boolean
}
